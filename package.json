{
  "name": "react-lastcall-hook",
  "version": "1.0.6",
  "description": "A react or react native library to call functions comparing the last time that it was called",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc --build",
    "clean": "npx tsc --build --clean",
    "tsc": "tsc -p tsconfig.json",
    "dev": "yarn build && node -e require('./src/test.js')"
  },
  "repository": "https://github.com/HubertRyanOfficial/react-lastcall-hook.git",
  "author": "Hubert Ryan (hubertryanofficial@gmail.com)",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "@types/node": "^17.0.21",
    "prettier": "^2.5.1",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  }
}
